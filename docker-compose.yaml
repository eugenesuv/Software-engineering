services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    command:
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - --db-path
      - /app/data/car_rental.db
      - --jwt-secret
      - docker-dev-secret
      - --manager-password
      - Manager123!
